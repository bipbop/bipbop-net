namespace BipbopNet
{
    public class Database : TargetDescription
    {
        public Database(string? name, string? description, string? url) : base(name, description, url)
        {
        }
    }
}